intensity<-c(intensity, 0.64)
intensity<-c(intensity, 0.64)
intensity<-c(intensity, 0.67)
intensity<-c(intensity, 0.69)
intensity<-c(intensity, 0.71)
intensity<-c(intensity, 0.72)
intensity<-c(intensity, 0.75)
intensity<-c(intensity, 0.78)
intensity<-c(intensity, 0.79)
intensity<-c(intensity, 0.74)
intensity<-c(intensity, 0.75)
intensity<-c(intensity, 0.73)
intensity<-c(intensity, 0.74)
intensity<-c(intensity, 0.73)
intensity<-c(intensity, 0.69)
intensity<-c(intensity, 0.69)
intensity<-c(intensity, 0.65)
intensity<-c(intensity, 0.68)
intensity<-c(intensity, 0.68)
intensity<-c(intensity, 0.67)
intensity<-c(intensity, 0.68)
intensity<-c(intensity, 0.67)
intensity<-c(intensity, 0.68)
intensity<-c(intensity, 0.68)
intensity<-c(intensity, 0.69)
?matri
?matrix
?array
?matrix
test<-matrix(runif(16,0,10), 4, 4)
test
range(test)
data.frame
?data.frame
?list
test<-list()
test
vector("list",10)
container<-vector("list",3)
container
container[[1]]<-matrix(runif(100,0,10),50,2)
container
container[[1]]<-cbind(container[[1]],runif(50,0,10))
container
container[[2]]<-matrix(runif(100,0,10),50,2)
container[[1]]<-matrix(runif(100,0,10),50,2)
container[[3]]<-matrix(runif(100,0,10),50,2)
container
plot(container)
plot(container[[1]])
range(container)
container[[1]]<-matrix(c(runif(50,0,10), runif(50, 100,200),50,2)
container[[1]]<-matrix(c(runif(50,0,10), runif(50, 100,200)),50,2)
container[[1]]<-matrix(c(runif(50,0,10), runif(50, 100,200)),50,2)
container[[2]]<-matrix(c(runif(50,0,10), runif(50, 100,200)),50,2)
container[[3]]<-matrix(c(runif(50,0,10), runif(50, 100,200)),50,2)
range(container)
range(container[[]][,1])
range(container[,1])
container[[]]
container[,1]
container[[1]][,1]
container
max(container)
range(container)
range(container[[1]])
range(container[[2]])
range(container[[3]])
range(container[[3]][,1])
range(container[[3]][,2])
timeList<-vector("list", 3)
signalList<-vector("list", 3)
timeList[[1]]<-c(timeList[[1]], 21,52)
timeList<-vector("list", 3)
signalList<-vector("list", 3)
timeList[[1]]<-c(timeList[[1]], 21,52)
timeList[[2]]<-c(timeList[[1]], 21.54)
timeList<-vector("list", 3)
signalList<-vector("list", 3)
timeList[[1]]<-c(timeList[[1]], 21,52)
timeList[[1]]<-c(timeList[[1]], 21,54)
timeList<-vector("list", 3)
signalList<-vector("list", 3)
timeList[[1]]<-c(timeList[[1]], 21.52)
timeList[[1]]<-c(timeList[[1]], 21.54)
timeList<-vector("list", 3)
signalList<-vector("list", 3)
timeList[[1]]<-c(timeList[[1]], 21.52)
timeList[[1]]<-c(timeList[[1]], 21.54)
timeList[[1]]<-c(timeList[[1]], 21.53)
timeList<-vector("list", 3)
signalList<-vector("list", 3)
timeList[[1]]<-c(timeList[[1]], 21.52)
timeList[[1]]<-c(timeList[[1]], 21.54)
timeList[[1]]<-c(timeList[[1]], 21.53)
timeList[[2]]<-c(timeList[[1]], 21.11)
timeList[[2]]<-c(timeList[[1]], 21.53)
plot(timeList[[1]], 1:3)
plot(timeList[[1]], 1:3, xlim=c(range(timeList)[1], range(timeList)[2]), ylim=c(1, 3))
points(timeList[[2]], 2:3)
2:3
timeList[[2]]
timeList<-vector("list", 3)
signalList<-vector("list", 3)
timeList[[1]]<-c(timeList[[1]], 21.52)
timeList[[1]]<-c(timeList[[1]], 21.54)
timeList[[1]]<-c(timeList[[1]], 21.53)
timeList[[2]]<-c(timeList[[2]], 21.11)
timeList[[2]]<-c(timeList[[2]], 21.53)
plot(timeList[[1]], 1:3, xlim=c(range(timeList)[1], range(timeList)[2]), ylim=c(1, 3))
points(timeList[[2]], 2:3)
plot(timeList[[1]], 1:3, xlim=c(range(timeList)[1], range(timeList)[2]), ylim=c(1, 3))
points(timeList[[2]], 2:3, col=sample(colours(), 1))
timeList<-vector("list", 3)
signalList<-vector("list", 3)
timeList[[1]]<-c(timeList[[1]], 21.52)
timeList[[1]]<-c(timeList[[1]], 21.54)
timeList[[1]]<-c(timeList[[1]], 21.53)
timeList[[2]]<-c(timeList[[2]], 21.11)
timeList[[2]]<-c(timeList[[2]], 21.53)
plot(timeList[[1]], 1:3, xlim=c(range(timeList)[1], range(timeList)[2]), ylim=c(1, 3), type='l')
points(timeList[[2]], 2:3, type='l', col=sample(colours(), 1))
timeList<-vector("list", 3)
signalList<-vector("list", 3)
timeList[[1]]<-c(timeList[[1]], 21.52)
timeList[[1]]<-c(timeList[[1]], 21.54)
timeList[[1]]<-c(timeList[[1]], 21.53)
timeList[[2]]<-c(timeList[[2]], 21.11)
timeList[[2]]<-c(timeList[[2]], 21.53)
plot(timeList[[1]], 1:3, xlim=c(range(timeList)[1], range(timeList)[2]), ylim=c(1, 3), type='l')
points(timeList[[2]], 2:3, type='l', col=sample(colours(), 1))
timeList<-vector("list", 3)
signalList<-vector("list", 3)
timeList[[1]]<-c(timeList[[1]], 21.52)
timeList[[1]]<-c(timeList[[1]], 21.54)
timeList[[1]]<-c(timeList[[1]], 21.53)
timeList[[2]]<-c(timeList[[2]], 21.11)
timeList[[2]]<-c(timeList[[2]], 21.53)
plot(timeList[[1]], 1:3, xlim=c(range(timeList)[1], range(timeList)[2]), ylim=c(1, 3), type='l', col=sample(colour(),1))
points(timeList[[2]], 2:3, type='l', col=sample(colours(), 1))
timeList<-vector("list", 3)
signalList<-vector("list", 3)
timeList[[1]]<-c(timeList[[1]], 21.52)
timeList[[1]]<-c(timeList[[1]], 21.54)
timeList[[1]]<-c(timeList[[1]], 21.53)
timeList[[2]]<-c(timeList[[2]], 21.11)
timeList[[2]]<-c(timeList[[2]], 21.53)
plot(timeList[[1]], 1:3, xlim=c(range(timeList)[1], range(timeList)[2]), ylim=c(1, 3), type='l', col=sample(colours(),1))
points(timeList[[2]], 2:3, type='l', col=sample(colours(), 1))
timeList<-vector("list", 3)
signalList<-vector("list", 3)
timeList[[1]]<-c(timeList[[1]], 21.52)
timeList[[1]]<-c(timeList[[1]], 21.54)
timeList[[1]]<-c(timeList[[1]], 21.53)
timeList[[2]]<-c(timeList[[2]], 21.11)
timeList[[2]]<-c(timeList[[2]], 21.53)
plot(timeList[[1]], 1:3, xlim=c(range(timeList)[1], range(timeList)[2]), ylim=c(1, 3), type='l', col=sample(colours(),1))
points(timeList[[2]], 2:3, type='l', col=sample(colours(), 1))
timeList<-vector("list", 3)
signalList<-vector("list", 3)
timeList[[1]]<-c(timeList[[1]], 21.52)
timeList[[1]]<-c(timeList[[1]], 21.54)
timeList[[1]]<-c(timeList[[1]], 21.53)
timeList[[2]]<-c(timeList[[2]], 21.11)
timeList[[2]]<-c(timeList[[2]], 21.53)
plot(timeList[[1]], 1:3, xlim=c(range(timeList)[1], range(timeList)[2]), ylim=c(1, 3), type='l', col=sample(colours(),1))
points(timeList[[2]], 2:3, type='l', col=sample(colours(), 1))
O0<-c(3.191973, 3.287341, 3.182491, 3.220779, 3.168903)
mean(O0)
O1<-c(1.000278, 1017462, 1.003534, 1.019629, 0.994709)
mean(O1)
O1<-c(1.000278, 1.017462, 1.003534, 1.019629, 0.994709)
mean(O1)
O2<-c(0.844032, 0.854150, 0.828825, 0.840293, 0.830058)
O3<-c(0.828782, 0.833252, 0.835590, 0.846589, 0.846393)
Os<-c(1.368225, 1.382401, 1.399216, 1.372827, 1.380438)
mean(O0)
round(mean(O0),2)
var(O0)
var(O0)^2
round(mean(O1),2)
round(mean(O2),2)
round(mean(O3),2)
round(mean(Os),2)
var(O0)^2
var(O1)^2
var(O2)^2
var(O3)^2
var(Os)^2
ls
source('~/Desktop/bullshit.R')
install.packages('RServe')
install.packages('Rserve')
20*4*5
20*4*5*3
library(dplyr)
# Load data
setwd("~/git/os_assignment_2/data_eval/")
benchmarking <- read.table('data.txt', sep=" ", stringsAsFactors = FALSE )
header<-c("scheduler", "load_type", "sort_size", "thread_rank", "cpu_time", "wall_time", "wall_create_to_run", "run_to_finish")
colnames(benchmarking)<-header
attach(benchmarking)
summary(benchmarking)
4*5*3
dim(benchmarking)
setwd("~/git/os_assignment_2/data_eval/")
per_run <- read.table('total_time.txt', sep=" ", stringsAsFactors = FALSE )
header<-c("scheduler", "load_type", "total_time")
colnames(per_run)<-header
attach(per_run)
setwd("~/git/os_assignment_2/data_eval/")
per_run <- read.table('total_time.txt', sep=" ", stringsAsFactors = FALSE )
header<-c("scheduler", "load_type", "total_time")
colnames(per_run)<-header
attach(per_run)
other <- filter(per_run, scheduler == "sched_other")
by_load_type<-group_by(other, load_type)
summarize(by_load_type,mean(cpu_time), mean(wall_time), mean(wall_create_to_run), mean(run_to_finish))
rr <- filter(per_run, scheduler == "sched_rr")
by_load_type<-group_by(rr, load_type)
summarize(by_load_type,mean(cpu_time), mean(wall_time), mean(wall_create_to_run), mean(run_to_finish))
fifo <- filter(per_run, scheduler == "sched_fifo")
by_load_type<-group_by(fifo, load_type)
summarize(by_load_type,mean(cpu_time), mean(wall_time), mean(wall_create_to_run), mean(run_to_finish))
other <- filter(per_run, scheduler == "sched_other")
by_load_type<-group_by(other, load_type)
summarize(by_load_type,mean(total_time))
rr <- filter(per_run, scheduler == "sched_rr")
by_load_type<-group_by(rr, load_type)
summarize(by_load_type,mean(total_time))
fifo <- filter(per_run, scheduler == "sched_fifo")
by_load_type<-group_by(fifo, load_type)
summarize(by_load_type,mean(total_time))
other <- filter(per_run, scheduler == "sched_other")
by_load_type<-group_by(other, load_type)
summarize(by_load_type,round(mean(total_time),2))
rr <- filter(per_run, scheduler == "sched_rr")
by_load_type<-group_by(rr, load_type)
summarize(by_load_type,round(mean(total_time),2))
fifo <- filter(per_run, scheduler == "sched_fifo")
by_load_type<-group_by(fifo, load_type)
summarize(by_load_type,round(mean(total_time),2))
other <- filter(per_run, scheduler == "sched_other")
by_load_type<-group_by(other, load_type)
summarize(by_load_type,"Per run" = round(mean(total_time),2))
rr <- filter(per_run, scheduler == "sched_rr")
by_load_type<-group_by(rr, load_type)
summarize(by_load_type,"Per run" = round(mean(total_time),2))
fifo <- filter(per_run, scheduler == "sched_fifo")
by_load_type<-group_by(fifo, load_type)
summarize(by_load_type,"Per run" = round(mean(total_time),2))
schedulers<-{"sched_other", "sched_rr", "sched_fifo"}
schedulers<-["sched_other", "sched_rr", "sched_fifo"]
schedulers<-c("sched_other", "sched_rr", "sched_fifo")
for(i in schedulers){
other <- filter(per_run, scheduler == i)
by_load_type<-group_by(other, load_type)
summarize(by_load_type,"Per run" = round(mean(total_time),2))
}
# RSD total time
other <- filter(per_run, scheduler == "sched_other")
by_load_type<-group_by(other, load_type)
summarize(by_load_type,"Per run" = round(100/mean(total_time)*sd(total_time),2))
rr <- filter(per_run, scheduler == "sched_rr")
by_load_type<-group_by(rr, load_type)
summarize(by_load_type,"Per run" = round(100/mean(total_time)*sd(total_time),2))
fifo <- filter(per_run, scheduler == "sched_fifo")
by_load_type<-group_by(fifo, load_type)
summarize(by_load_type,"Per run" = round(100/mean(total_time)*sd(total_time),2))
setwd("~/git/os_assignment_2/data_eval/")
per_thread <- read.table('data.txt', sep=" ", stringsAsFactors = FALSE )
header<-c("scheduler", "load_type", "sort_size", "thread_rank", "cpu_time", "wall_time", "wall_create_to_run", "run_to_finish")
colnames(per_thread)<-header
attach(per_thread)
other_inc <- filter(per_thread, scheduler == "sched_other", load_type == "incr")
by_rank<-group_by(other_inc, thread_rank)
summarize(by_rank, sd(cpu_time), sd(wall_time), sd(wall_create_to_run), sd(run_to_finish))
summarize(by_rank, mean(cpu_time), mean(wall_time), mean(wall_create_to_run), mean(run_to_finish))
summarize(by_rank, 'RSD cpu time' = 100/mean(cpu_time)*sd(cpu_time),
'RSD wall time' = 100/mean(wall_time)*sd(wall_time),
'RSD create to run' = 100/mean(wall_create_to_run)*sd(wall_create_to_run),
'RSD run to finish' = 100/mean(run_to_finish)*sd(run_to_finish))
other <- filter(per_thread, scheduler == "sched_other")
by_load_type<-group_by(other, load_type)
summarize(by_load_type,mean(cpu_time), mean(wall_time), mean(wall_create_to_run), mean(run_to_finish))
rr <- filter(per_thread, scheduler == "sched_rr")
by_load_type<-group_by(rr, load_type)
summarize(by_load_type,mean(cpu_time), mean(wall_time), mean(wall_create_to_run), mean(run_to_finish))
fifo <- filter(per_thread, scheduler == "sched_fifo")
by_load_type<-group_by(fifo, load_type)
summarize(by_load_type,mean(cpu_time), mean(wall_time), mean(wall_create_to_run), mean(run_to_finish))
other <- filter(per_thread, scheduler == "sched_other")
by_load_type<-group_by(other, load_type)
summarize(by_load_type,range(cpu_time), range(wall_time), range(wall_create_to_run), range(run_to_finish))
other <- filter(per_thread, scheduler == "sched_other")
by_load_type<-group_by(other, load_type)
summarize(by_load_type,range(cpu_time)[2]-range(cpu_time)[1], range(wall_time)[2] - range(wall_time)[1], range(wall_create_to_run)[2] -  range(wall_create_to_run)[1], range(run_to_finish)[2] - range(run_to_finish)[1])
other <- filter(per_thread, scheduler == "sched_other")
by_load_type<-group_by(other, load_type)
summarize(by_load_type, "range cpu-time (s)" = range(cpu_time)[2]-range(cpu_time)[1], "range wall time (s)" = range(wall_time)[2] - range(wall_time)[1], "range create-to-run (s)" = range(wall_create_to_run)[2] -  range(wall_create_to_run)[1], "range run-to-finish" = range(run_to_finish)[2] - range(run_to_finish)[1])
other <- filter(per_thread, scheduler == "sched_other")
by_load_type<-group_by(other, load_type)
summarize(by_load_type, "range cpu-time (s)" = range(cpu_time)[2]-range(cpu_time)[1], "range wall time (s)" = range(wall_time)[2] - range(wall_time)[1], "range create-to-run (s)" = range(wall_create_to_run)[2] -  range(wall_create_to_run)[1], "range run-to-finish" = range(run_to_finish)[2] - range(run_to_finish)[1])
other <- filter(per_thread, scheduler == "sched_rr")
by_load_type<-group_by(other, load_type)
summarize(by_load_type, "range cpu-time (s)" = range(cpu_time)[2]-range(cpu_time)[1], "range wall time (s)" = range(wall_time)[2] - range(wall_time)[1], "range create-to-run (s)" = range(wall_create_to_run)[2] -  range(wall_create_to_run)[1], "range run-to-finish" = range(run_to_finish)[2] - range(run_to_finish)[1])
other <- filter(per_thread, scheduler == "sched_fifo")
by_load_type<-group_by(other, load_type)
summarize(by_load_type, "range cpu-time (s)" = range(cpu_time)[2]-range(cpu_time)[1], "range wall time (s)" = range(wall_time)[2] - range(wall_time)[1], "range create-to-run (s)" = range(wall_create_to_run)[2] -  range(wall_create_to_run)[1], "range run-to-finish" = range(run_to_finish)[2] - range(run_to_finish)[1])
other <- filter(per_thread, scheduler == "sched_other")
by_load_type<-group_by(other, load_type)
summarize(by_load_type,mean(cpu_time), mean(wall_time), mean(wall_create_to_run), mean(run_to_finish))
rr <- filter(per_thread, scheduler == "sched_rr")
by_load_type<-group_by(rr, load_type)
summarize(by_load_type,mean(cpu_time), mean(wall_time), mean(wall_create_to_run), mean(run_to_finish))
fifo <- filter(per_thread, scheduler == "sched_fifo")
by_load_type<-group_by(fifo, load_type)
summarize(by_load_type,mean(cpu_time), mean(wall_time), mean(wall_create_to_run), mean(run_to_finish))
other <- filter(per_thread, scheduler == "sched_other")
by_load_type<-group_by(other, load_type)
summarize(by_load_type, round(mean(cpu_time),2), round(mean(wall_time), 2), round(mean(wall_create_to_run),2), round(mean(run_to_finish),2))
rr <- filter(per_thread, scheduler == "sched_rr")
by_load_type<-group_by(rr, load_type)
summarize(by_load_type, round(mean(cpu_time),2), round(mean(wall_time), 2), round(mean(wall_create_to_run),2), round(mean(run_to_finish),2))
fifo <- filter(per_thread, scheduler == "sched_fifo")
by_load_type<-group_by(fifo, load_type)
summarize(by_load_type, round(mean(cpu_time),2), round(mean(wall_time), 2), round(mean(wall_create_to_run),2), round(mean(run_to_finish),2))
other <- filter(per_thread, scheduler == "sched_other")
by_load_type<-group_by(other, load_type)
summarize(by_load_type, "range cpu-time (s)" = range(cpu_time)[2]-range(cpu_time)[1], "range wall time (s)" = range(wall_time)[2] - range(wall_time)[1], "range create-to-run (s)" = range(wall_create_to_run)[2] -  range(wall_create_to_run)[1], "range run-to-finish" = range(run_to_finish)[2] - range(run_to_finish)[1])
other <- filter(per_thread, scheduler == "sched_rr")
by_load_type<-group_by(other, load_type)
summarize(by_load_type, "range cpu-time (s)" = range(cpu_time)[2]-range(cpu_time)[1], "range wall time (s)" = range(wall_time)[2] - range(wall_time)[1], "range create-to-run (s)" = range(wall_create_to_run)[2] -  range(wall_create_to_run)[1], "range run-to-finish" = range(run_to_finish)[2] - range(run_to_finish)[1])
other <- filter(per_thread, scheduler == "sched_fifo")
by_load_type<-group_by(other, load_type)
summarize(by_load_type, "range cpu-time (s)" = range(cpu_time)[2]-range(cpu_time)[1], "range wall time (s)" = range(wall_time)[2] - range(wall_time)[1], "range create-to-run (s)" = range(wall_create_to_run)[2] -  range(wall_create_to_run)[1], "range run-to-finish" = range(run_to_finish)[2] - range(run_to_finish)[1])
other <- filter(per_thread, scheduler == "sched_other")
by_load_type<-group_by(other, load_type)
summarize(by_load_type, "range cpu-time (s)" = range(cpu_time)[2]-range(cpu_time)[1], "range wall time (s)" = round(range(wall_time)[2] - range(wall_time)[1], 2), "range create-to-run (s)" = round(range(wall_create_to_run)[2] -  range(wall_create_to_run)[1], 2), "range run-to-finish" = round(range(run_to_finish)[2] - range(run_to_finish)[1], 2))
other <- filter(per_thread, scheduler == "sched_rr")
by_load_type<-group_by(other, load_type)
summarize(by_load_type, "range cpu-time (s)" = range(cpu_time)[2]-range(cpu_time)[1], "range wall time (s)" = round(range(wall_time)[2] - range(wall_time)[1], 2), "range create-to-run (s)" = round(range(wall_create_to_run)[2] -  range(wall_create_to_run)[1], 2), "range run-to-finish" = round(range(run_to_finish)[2] - range(run_to_finish)[1], 2))
other <- filter(per_thread, scheduler == "sched_fifo")
by_load_type<-group_by(other, load_type)
summarize(by_load_type, "range cpu-time (s)" = range(cpu_time)[2]-range(cpu_time)[1], "range wall time (s)" = round(range(wall_time)[2] - range(wall_time)[1], 2), "range create-to-run (s)" = round(range(wall_create_to_run)[2] -  range(wall_create_to_run)[1], 2), "range run-to-finish" = round(range(run_to_finish)[2] - range(run_to_finish)[1], 2))
other <- filter(per_thread, scheduler == "sched_other")
by_load_type<-group_by(other, load_type)
summarize(by_load_type, "range cpu-time (s)" = round(range(cpu_time)[2]-range(cpu_time)[1], 2), "range wall time (s)" = round(range(wall_time)[2] - range(wall_time)[1], 2), "range create-to-run (s)" = round(range(wall_create_to_run)[2] -  range(wall_create_to_run)[1], 2), "range run-to-finish" = round(range(run_to_finish)[2] - range(run_to_finish)[1], 2))
other <- filter(per_thread, scheduler == "sched_rr")
by_load_type<-group_by(other, load_type)
summarize(by_load_type, "range cpu-time (s)" = round(range(cpu_time)[2]-range(cpu_time)[1], 2), "range wall time (s)" = round(range(wall_time)[2] - range(wall_time)[1], 2), "range create-to-run (s)" = round(range(wall_create_to_run)[2] -  range(wall_create_to_run)[1], 2), "range run-to-finish" = round(range(run_to_finish)[2] - range(run_to_finish)[1], 2))
other <- filter(per_thread, scheduler == "sched_fifo")
by_load_type<-group_by(other, load_type)
summarize(by_load_type, "range cpu-time (s)" = round(range(cpu_time)[2]-range(cpu_time)[1], 2), "range wall time (s)" = round(range(wall_time)[2] - range(wall_time)[1], 2), "range create-to-run (s)" = round(range(wall_create_to_run)[2] -  range(wall_create_to_run)[1], 2), "range run-to-finish" = round(range(run_to_finish)[2] - range(run_to_finish)[1], 2))
other <- filter(per_thread, scheduler == "sched_other")
by_load_type<-group_by(other, load_type)
summarize(by_load_type, round(mean(cpu_time),2), round(mean(wall_time), 2), round(mean(wall_create_to_run),2), round(mean(run_to_finish),2))
rr <- filter(per_thread, scheduler == "sched_rr")
by_load_type<-group_by(rr, load_type)
summarize(by_load_type, round(mean(cpu_time),2), round(mean(wall_time), 2), round(mean(wall_create_to_run),2), round(mean(run_to_finish),2))
fifo <- filter(per_thread, scheduler == "sched_fifo")
by_load_type<-group_by(fifo, load_type)
summarize(by_load_type, round(mean(cpu_time),2), round(mean(wall_time), 2), round(mean(wall_create_to_run),2), round(mean(run_to_finish),2))
other <- filter(per_thread, scheduler == "sched_other")
by_load_type<-group_by(other, load_type)
summarize(by_load_type, "mean cpu time" = round(mean(cpu_time),2), "mean cpu time" = round(mean(wall_time), 2), "mean create-to-run" = round(mean(wall_create_to_run),2), "mean run-to-finish" = round(mean(run_to_finish),2))
rr <- filter(per_thread, scheduler == "sched_rr")
by_load_type<-group_by(rr, load_type)
summarize(by_load_type, "mean cpu time" = round(mean(cpu_time),2), "mean cpu time" = round(mean(wall_time), 2), "mean create-to-run" = round(mean(wall_create_to_run),2), "mean run-to-finish" = round(mean(run_to_finish),2))
fifo <- filter(per_thread, scheduler == "sched_fifo")
by_load_type<-group_by(fifo, load_type)
summarize(by_load_type, "mean cpu time" = round(mean(cpu_time),2), "mean cpu time" = round(mean(wall_time), 2), "mean create-to-run" = round(mean(wall_create_to_run),2), "mean run-to-finish" = round(mean(run_to_finish),2))
other <- filter(per_thread, scheduler == "sched_other")
by_load_type<-group_by(other, load_type)
summarize(by_load_type, "mean cpu time" = round(mean(cpu_time),2), "mean cpu time" = round(mean(wall_time), 2), "mean create-to-run" = round(mean(wall_create_to_run),2), "mean run-to-finish" = round(mean(run_to_finish),2))
rr <- filter(per_thread, scheduler == "sched_rr")
by_load_type<-group_by(rr, load_type)
summarize(by_load_type, "mean cpu time" = round(mean(cpu_time),2), "mean cpu time" = round(mean(wall_time), 2), "mean create-to-run" = round(mean(wall_create_to_run),2), "mean run-to-finish" = round(mean(run_to_finish),2))
fifo <- filter(per_thread, scheduler == "sched_fifo")
by_load_type<-group_by(fifo, load_type)
summarize(by_load_type, "mean cpu time" = round(mean(cpu_time),2), "mean cpu time" = round(mean(wall_time), 2), "mean create-to-run" = round(mean(wall_create_to_run),2), "mean run-to-finish" = round(mean(run_to_finish),2))
other <- filter(per_thread, scheduler == "sched_other")
by_load_type<-group_by(other, load_type)
summarize(by_load_type, "mean cpu time" = round(mean(cpu_time),2), "mean wall time" = round(mean(wall_time), 2), "mean create-to-run" = round(mean(wall_create_to_run),2), "mean run-to-finish" = round(mean(run_to_finish),2))
rr <- filter(per_thread, scheduler == "sched_rr")
by_load_type<-group_by(rr, load_type)
summarize(by_load_type, "mean cpu time" = round(mean(cpu_time),2), "mean wall time" = round(mean(wall_time), 2), "mean create-to-run" = round(mean(wall_create_to_run),2), "mean run-to-finish" = round(mean(run_to_finish),2))
fifo <- filter(per_thread, scheduler == "sched_fifo")
by_load_type<-group_by(fifo, load_type)
summarize(by_load_type, "mean cpu time" = round(mean(cpu_time),2), "mean wall time" = round(mean(wall_time), 2), "mean create-to-run" = round(mean(wall_create_to_run),2), "mean run-to-finish" = round(mean(run_to_finish),2))
other <- filter(per_thread, scheduler == "sched_other")
by_load_type<-group_by(other, load_type)
summarize(by_load_type, "range cpu-time (s)" = round(range(cpu_time)[2]-range(cpu_time)[1], 2), "range wall time (s)" = round(range(wall_time)[2] - range(wall_time)[1], 2), "range create-to-run (s)" = round(range(wall_create_to_run)[2] -  range(wall_create_to_run)[1], 2), "range run-to-finish" = round(range(run_to_finish)[2] - range(run_to_finish)[1], 2))
other <- filter(per_thread, scheduler == "sched_rr")
by_load_type<-group_by(other, load_type)
summarize(by_load_type, "range cpu-time (s)" = round(range(cpu_time)[2]-range(cpu_time)[1], 2), "range wall time (s)" = round(range(wall_time)[2] - range(wall_time)[1], 2), "range create-to-run (s)" = round(range(wall_create_to_run)[2] -  range(wall_create_to_run)[1], 2), "range run-to-finish" = round(range(run_to_finish)[2] - range(run_to_finish)[1], 2))
other <- filter(per_thread, scheduler == "sched_fifo")
by_load_type<-group_by(other, load_type)
summarize(by_load_type, "range cpu-time (s)" = round(range(cpu_time)[2]-range(cpu_time)[1], 2), "range wall time (s)" = round(range(wall_time)[2] - range(wall_time)[1], 2), "range create-to-run (s)" = round(range(wall_create_to_run)[2] -  range(wall_create_to_run)[1], 2), "range run-to-finish" = round(range(run_to_finish)[2] - range(run_to_finish)[1], 2))
other <- filter(per_thread, scheduler == "sched_other")
by_load_type<-group_by(other, load_type)
summarize(by_load_type, "mean cpu time" = round(mean(cpu_time),2), "mean wall time" = round(mean(wall_time), 2), "mean create-to-run" = round(mean(wall_create_to_run),2), "mean run-to-finish" = round(mean(run_to_finish),2))
rr <- filter(per_thread, scheduler == "sched_rr")
by_load_type<-group_by(rr, load_type)
summarize(by_load_type, "mean cpu time" = round(mean(cpu_time),2), "mean wall time" = round(mean(wall_time), 2), "mean create-to-run" = round(mean(wall_create_to_run),2), "mean run-to-finish" = round(mean(run_to_finish),2))
fifo <- filter(per_thread, scheduler == "sched_fifo")
by_load_type<-group_by(fifo, load_type)
summarize(by_load_type, "mean cpu time" = round(mean(cpu_time),2), "mean wall time" = round(mean(wall_time), 2), "mean create-to-run" = round(mean(wall_create_to_run),2), "mean run-to-finish" = round(mean(run_to_finish),2))
other <- filter(per_thread, scheduler == "sched_other")
by_load_type<-group_by(other, load_type)
summarize(by_load_type, "range cpu-time (s)" = round(range(cpu_time)[2]-range(cpu_time)[1], 2), "range wall time (s)" = round(range(wall_time)[2] - range(wall_time)[1], 2), "range create-to-run (s)" = round(range(wall_create_to_run)[2] -  range(wall_create_to_run)[1], 2), "range run-to-finish" = round(range(run_to_finish)[2] - range(run_to_finish)[1], 2))
other <- filter(per_thread, scheduler == "sched_rr")
by_load_type<-group_by(other, load_type)
summarize(by_load_type, "range cpu-time (s)" = round(range(cpu_time)[2]-range(cpu_time)[1], 2), "range wall time (s)" = round(range(wall_time)[2] - range(wall_time)[1], 2), "range create-to-run (s)" = round(range(wall_create_to_run)[2] -  range(wall_create_to_run)[1], 2), "range run-to-finish" = round(range(run_to_finish)[2] - range(run_to_finish)[1], 2))
other <- filter(per_thread, scheduler == "sched_fifo")
by_load_type<-group_by(other, load_type)
summarize(by_load_type, "range cpu-time (s)" = round(range(cpu_time)[2]-range(cpu_time)[1], 2), "range wall time (s)" = round(range(wall_time)[2] - range(wall_time)[1], 2), "range create-to-run (s)" = round(range(wall_create_to_run)[2] -  range(wall_create_to_run)[1], 2), "range run-to-finish" = round(range(run_to_finish)[2] - range(run_to_finish)[1], 2))
other_inc <- filter(per_thread, scheduler == "sched_other", load_type == "incr")
by_rank<-group_by(other_inc, thread_rank)
summarize(by_rank, sd(cpu_time), sd(wall_time), sd(wall_create_to_run), sd(run_to_finish))
summarize(by_rank, mean(cpu_time), mean(wall_time), mean(wall_create_to_run), mean(run_to_finish))
summarize(by_rank, 'RSD cpu time' = 100/mean(cpu_time)*sd(cpu_time),
'RSD wall time' = 100/mean(wall_time)*sd(wall_time),
'RSD create to run' = 100/mean(wall_create_to_run)*sd(wall_create_to_run),
'RSD run to finish' = 100/mean(run_to_finish)*sd(run_to_finish))
rr_inc <- filter(per_thread, scheduler == "sched_rr", load_type == "incr")
by_rank<-group_by(rr_inc, thread_rank)
summarize(by_rank, sd(cpu_time), sd(wall_time), sd(wall_create_to_run), sd(run_to_finish))
summarize(by_rank, mean(cpu_time), mean(wall_time), mean(wall_create_to_run), mean(run_to_finish))
summarize(by_rank, 'RSD cpu time' = 100/mean(cpu_time)*sd(cpu_time),
'RSD wall time' = 100/mean(wall_time)*sd(wall_time),
'RSD create to run' = 100/mean(wall_create_to_run)*sd(wall_create_to_run),
'RSD run to finish' = 100/mean(run_to_finish)*sd(run_to_finish))
fifo_inc <- filter(per_thread, scheduler == "sched_fifo", load_type == "incr")
by_rank<-group_by(rr_inc, thread_rank)
summarize(by_rank, sd(cpu_time), sd(wall_time), sd(wall_create_to_run), sd(run_to_finish))
summarize(by_rank, mean(cpu_time), mean(wall_time), mean(wall_create_to_run), mean(run_to_finish))
summarize(by_rank, 'RSD cpu time' = 100/mean(cpu_time)*sd(cpu_time),
'RSD wall time' = 100/mean(wall_time)*sd(wall_time),
'RSD create to run' = 100/mean(wall_create_to_run)*sd(wall_create_to_run),
'RSD run to finish' = 100/mean(run_to_finish)*sd(run_to_finish))
other <- filter(per_thread, scheduler == "sched_other")
by_load_type<-group_by(other, load_type)
summarize(by_load_type, "mean cpu time" = round(sd(cpu_time),2), "mean wall time" = round(sd(wall_time), 2), "mean create-to-run" = round(sd(wall_create_to_run),2), "mean run-to-finish" = round(sd(run_to_finish),2))
rr <- filter(per_thread, scheduler == "sched_rr")
by_load_type<-group_by(rr, load_type)
summarize(by_load_type, "mean cpu time" = round(sd(cpu_time),2), "mean wall time" = round(sd(wall_time), 2), "mean create-to-run" = round(sd(wall_create_to_run),2), "mean run-to-finish" = round(sd(run_to_finish),2))
fifo <- filter(per_thread, scheduler == "sched_fifo")
by_load_type<-group_by(fifo, load_type)
summarize(by_load_type, "mean cpu time" = round(sd(cpu_time),2), "mean wall time" = round(sd(wall_time), 2), "mean create-to-run" = round(sd(wall_create_to_run),2), "mean run-to-finish" = round(sd(run_to_finish),2))
other <- filter(per_thread, scheduler == "sched_other")
by_load_type<-group_by(other, load_type)
summarize(by_load_type, "RSD cpu time" = round(100/mean(cpu_time) * sd(cpu_time),2), "RSD wall time" = round(100/mean(wall_time)*sd(wall_time), 2), "RSD create-to-run" = round(100/mean(wall_create_to_run)*sd(wall_create_to_run),2), "RSD run-to-finish" = round(100 / mean (run_to_finish) *sd(run_to_finish),2))
rr <- filter(per_thread, scheduler == "sched_rr")
by_load_type<-group_by(rr, load_type)
summarize(by_load_type, "RSD cpu time" = round(100/mean(cpu_time) * sd(cpu_time),2), "RSD wall time" = round(100/mean(wall_time)*sd(wall_time), 2), "RSD create-to-run" = round(100/mean(wall_create_to_run)*sd(wall_create_to_run),2), "RSD run-to-finish" = round(100 / mean (run_to_finish) *sd(run_to_finish),2))
fifo <- filter(per_thread, scheduler == "sched_fifo")
by_load_type<-group_by(fifo, load_type)
summarize(by_load_type, "RSD cpu time" = round(100/mean(cpu_time) * sd(cpu_time),2), "RSD wall time" = round(100/mean(wall_time)*sd(wall_time), 2), "RSD create-to-run" = round(100/mean(wall_create_to_run)*sd(wall_create_to_run),2), "RSD run-to-finish" = round(100 / mean (run_to_finish) *sd(run_to_finish),2))
other_inc <- filter(per_thread, scheduler == "sched_other", load_type == "incr")
by_rank<-group_by(other_inc, thread_rank)
summarize(by_rank, sd(cpu_time), sd(wall_time), sd(wall_create_to_run), sd(run_to_finish))
summarize(by_rank, mean(cpu_time), mean(wall_time), mean(wall_create_to_run), mean(run_to_finish))
summarize(by_rank, 'RSD cpu time' = 100/mean(cpu_time)*sd(cpu_time),
'RSD wall time' = 100/mean(wall_time)*sd(wall_time),
'RSD create to run' = 100/mean(wall_create_to_run)*sd(wall_create_to_run),
'RSD run to finish' = 100/mean(run_to_finish)*sd(run_to_finish))
rr_inc <- filter(per_thread, scheduler == "sched_rr", load_type == "incr")
by_rank<-group_by(rr_inc, thread_rank)
summarize(by_rank, sd(cpu_time), sd(wall_time), sd(wall_create_to_run), sd(run_to_finish))
summarize(by_rank, mean(cpu_time), mean(wall_time), mean(wall_create_to_run), mean(run_to_finish))
summarize(by_rank, 'RSD cpu time' = 100/mean(cpu_time)*sd(cpu_time),
'RSD wall time' = 100/mean(wall_time)*sd(wall_time),
'RSD create to run' = 100/mean(wall_create_to_run)*sd(wall_create_to_run),
'RSD run to finish' = 100/mean(run_to_finish)*sd(run_to_finish))
fifo_inc <- filter(per_thread, scheduler == "sched_fifo", load_type == "incr")
by_rank<-group_by(rr_inc, thread_rank)
summarize(by_rank, sd(cpu_time), sd(wall_time), sd(wall_create_to_run), sd(run_to_finish))
summarize(by_rank, mean(cpu_time), mean(wall_time), mean(wall_create_to_run), mean(run_to_finish))
summarize(by_rank, 'RSD cpu time' = 100/mean(cpu_time)*sd(cpu_time),
'RSD wall time' = 100/mean(wall_time)*sd(wall_time),
'RSD create to run' = 100/mean(wall_create_to_run)*sd(wall_create_to_run),
'RSD run to finish' = 100/mean(run_to_finish)*sd(run_to_finish))
summarize(by_rank, 'RSD cpu time' = 100/mean(cpu_time)*sd(cpu_time),
'RSD wall time' = 100/mean(wall_time)*sd(wall_time),
'RSD create to run' = 100/mean(wall_create_to_run)*sd(wall_create_to_run),
'RSD run to finish' = 100/mean(run_to_finish)*sd(run_to_finish))
rr_inc <- filter(per_thread, scheduler == "sched_rr", load_type == "incr")
by_rank<-group_by(rr_inc, thread_rank)
summarize(by_rank, sd(cpu_time), sd(wall_time), sd(wall_create_to_run), sd(run_to_finish))
summarize(by_rank, mean(cpu_time), mean(wall_time), mean(wall_create_to_run), mean(run_to_finish))
summarize(by_rank, 'RSD cpu time' = 100/mean(cpu_time)*sd(cpu_time),
'RSD wall time' = 100/mean(wall_time)*sd(wall_time),
'RSD create to run' = 100/mean(wall_create_to_run)*sd(wall_create_to_run),
'RSD run to finish' = 100/mean(run_to_finish)*sd(run_to_finish))
fifo_inc <- filter(per_thread, scheduler == "sched_fifo", load_type == "incr")
by_rank<-group_by(rr_inc, thread_rank)
summarize(by_rank, sd(cpu_time), sd(wall_time), sd(wall_create_to_run), sd(run_to_finish))
summarize(by_rank, mean(cpu_time), mean(wall_time), mean(wall_create_to_run), mean(run_to_finish))
summarize(by_rank, 'RSD cpu time' = 100/mean(cpu_time)*sd(cpu_time),
'RSD wall time' = 100/mean(wall_time)*sd(wall_time),
'RSD create to run' = 100/mean(wall_create_to_run)*sd(wall_create_to_run),
'RSD run to finish' = 100/mean(run_to_finish)*sd(run_to_finish))
other <- filter(per_thread, scheduler == "sched_other")
by_load_type<-group_by(other, load_type)
summarize(by_load_type, "mean cpu time" = round(mean(cpu_time),2), "mean wall time" = round(mean(wall_time), 2), "mean create-to-run" = round(mean(wall_create_to_run),2), "mean run-to-finish" = round(mean(run_to_finish),2))
rr <- filter(per_thread, scheduler == "sched_rr")
by_load_type<-group_by(rr, load_type)
summarize(by_load_type, "mean cpu time" = round(mean(cpu_time),2), "mean wall time" = round(mean(wall_time), 2), "mean create-to-run" = round(mean(wall_create_to_run),2), "mean run-to-finish" = round(mean(run_to_finish),2))
fifo <- filter(per_thread, scheduler == "sched_fifo")
by_load_type<-group_by(fifo, load_type)
summarize(by_load_type, "mean cpu time" = round(mean(cpu_time),2), "mean wall time" = round(mean(wall_time), 2), "mean create-to-run" = round(mean(wall_create_to_run),2), "mean run-to-finish" = round(mean(run_to_finish),2))
